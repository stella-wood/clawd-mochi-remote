# ── 凭据：绝不进版本库 ──────────────────────────────
secrets.h
credentials.h
.env
.env.*
.dev.vars

# ── Arduino / ESP32 编译产物 ────────────────────────
build/
*.bin
*.elf
*.hex
*.map
*.d
*.o

# ── IDE ─────────────────────────────────────────────
.theia/
.vscode/
.idea/

# ── Cloudflare Worker ───────────────────────────────
node_modules/
.wrangler/
dist/

# ── 系统 ────────────────────────────────────────────
.DS_Store
Thumbs.db
*~

# ── 本地施工用，不进版本库 ──────────────────────────
#  备份不推：回退是 git 自己的活，再存一份是冗余。
#  方案文档不推：那是施工稿，不是项目的一部分。
backup/
施工方案-*.md
