# ── 凭据：绝不进版本库 ──────────────────────────────
secrets.h
credentials.h
.env
.env.*
.dev.vars
MQTT.txt

# ── Arduino / ESP32 编译产物 ────────────────────────
build/
*.bin
*.elf
*.hex
*.map
*.d
*.o

# ── IDE ─────────────────────────────────────────────
.theia/
.vscode/
.idea/

# ── Cloudflare Worker ───────────────────────────────
node_modules/
.wrangler/
dist/

# ── 系统 ────────────────────────────────────────────
.DS_Store
Thumbs.db
*~

# ── others ──────────────────────────
clawd_mochi.3mf
backup/

# ── 文档：默认全部排除，只放行下面这三份 ────────────
# 2026-09-04 改成白名单制。原来是一条条列要排除的（施工方案-*.md），
# 每写一份新文档就得回来加一行，早晚会漏一份不该公开的进来。
# 现在反过来：默认不进库，要进的自己写在下面。
*.md
!README.md
!docs/AGENTS.md
!docs/TECHNICAL.md
